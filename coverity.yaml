capture:
  files:
    exclude-regex: '((\.venv|__pycache__|\.git)/.*)'
analyze:
  cov-analyze-args:
    - '--webapp-security'
    - '--security'
    - '--enable'
    - 'HARDCODED_CREDENTIALS'
    - '--enable'
    - 'SQLI'
    - '--enable'
    - 'OS_CMD_INJECTION'
